<main>
    <section id="deatils-page">
        <div class="wrapper">
            <div class="product-img">
                <img src="{{imageUrl}}">
            </div>
            <div class="product-info">
                <div class="product-text">
                    <div class="product-text">
                        <h1>Name: {{name}}</h1>
                        <h2>Type: {{type}}</h2>
                        <h4>Year: {{year}}</h4>
                        <h4>City: {{city}}</h4>
                        <p>Description: {{description}}</p>

                        <!-- if there are already tenants of the housing, separate their names with a comma and a space ", "  -->
                        {{#if isTenants}}
                        <p>People rented this housing: {{isTenants}}</p>
                        {{else}}
                        <p>People rented this housing: There are no tenants yet.</p>
                        {{/if}}


                        <!-- If not display: -->

                    </div>
                </div>

                <!-- if there is no registered user, do not display buttons-->
                <div class="product-btn">
                    {{#if user}}
                    {{#if isOwner}}
                    <a href="/products/edit/{{_id}}" class="edit">Edit</a>
                    <a href="/products/delete/{{_id}}" class="remove">Delete</a>
                    {{/if }}
                    {{#if isPlace}}
                    <a href="/products/rentHouse/{{_id}}" class="rentHome">Rent a home, available {{availablePieces}}
                        housing</a>
                    {{else}}
                    <p class="no-housing">No Housing Available!</p>
                    {{/if}}
                    {{else}}
                    <!-- Only for registered user and creator of the housing offer-->

                    <!-- logged in user with available pieces-->
                    {{#if isRented}}
                    <p class="alRentHome">You have already rent this home</p>
                    {{/if}}


                    <!-- logged in user with no available pieces-->

                </div>
                {{/if}}
            </div>
        </div>
    </section>
</main>